# Printer bridge abstraction

Implement printer behavior behind an interface:
- `printReceipt(orderPayload)`
- `printTestPage()`
- `getPrinterStatus()`

The concrete Sunmi implementation should live in native Android layer with Sunmi SDK binding.
